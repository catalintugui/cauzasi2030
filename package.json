{
  "name": "cauzasi2030",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "engines": {
    "node": ">=20.19.0"
  },
	"scripts": {
		"dev": "vite --open /",
		"build": "tsc --noEmit && vite build && node scripts/create-404.mjs",
		"build:prd": "tsc --noEmit && vite build --base / && node scripts/create-404.mjs --root",
		"preview": "vite preview --open /cauzasi2030/ --base /cauzasi2030/",
		"preview:prd": "vite preview --open / --base /"
	},

  "dependencies": {
    "react": "latest",
    "react-dom": "latest",
    "react-router-dom": "^7.15.1"
  },
  "devDependencies": {
    "@types/react": "latest",
    "@types/react-dom": "latest",
    "@vitejs/plugin-react": "latest",
    "sharp": "^0.35.2",
    "typescript": "latest",
    "vite": "latest"
  }
}
